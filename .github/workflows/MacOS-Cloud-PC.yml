- name: Enable VNC (Password Only – Secure)
  run: |
    sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart \
      -activate -configure -access -on \
      -privs -all \
      -restart -agent -menu

    # Enable VNC password authentication
    sudo defaults write /Library/Preferences/com.apple.VNCSettings AuthMethod -int 1

- name: Set VNC Password
  run: |
    echo "irvyrvck" | perl -we 'print pack("H*", unpack("H*", <>))' | sudo tee /Library/Preferences/com.apple.VNCSettings >/dev/null      - name: Keep Mac Alive
        uses: mxschmitt/action-tmate@v2
